import React from 'react';

import { LogoContainer, LogoName, LogoIcon } from './styled';

export default function Logo() {
  return (
    <LogoContainer>
      <LogoIcon />
      <LogoName />
    </LogoContainer>
  );
}
